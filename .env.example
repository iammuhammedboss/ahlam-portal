# Database
DATABASE_URL="postgresql://user:password@localhost:5432/adl_portal"

# Agent credentials (single agent)
AGENT_USERNAME="admin"
AGENT_PASSWORD_HASH=""  # Generate with: node -e "require('bcrypt').hash('yourpassword', 10).then(console.log)"

# Session
SESSION_SECRET=""  # Random 32+ character string

# Email (Google Workspace SMTP)
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_USER="no-reply@ahlamdhofar.com"
SMTP_PASS=""  # Google App Password
SMTP_FROM="Ahlam Dhofar Logistics <no-reply@ahlamdhofar.com>"

# Agent notification email
AGENT_NOTIFICATION_EMAIL="agent@ahlamdhofar.com"

# App
NEXT_PUBLIC_APP_URL="https://portal.ahlamdhofar.com"
UPLOAD_DIR="./public/uploads"
MAX_FILE_SIZE_MB="15"
